package org.HelpTools.trashPrototype;

public class HelpTools {
/*
  public static void resetPassword(String serverType, String server1C, String base1C, 
        String user1C, String newPassword1C, String dbServer, String dbBase, 
        String dbUser, String dbPassword){
              
    Properties connectionProps = new Properties();
    connectionProps.put("server1C", server1C);
    connectionProps.put("base1C", base1C);
    connectionProps.put("user1C", user1C);
    connectionProps.put("newPassword1C", newPassword1C);
    connectionProps.put("server", dbServer);
    connectionProps.put("database", dbBase);
    connectionProps.put("user", dbUser);
    connectionProps.put("password", dbPassword);
    
    QueryManager.DBProviders provider = null;
    try{ 
      provider = QueryManager.DBProviders.valueOf(serverType);}
    catch(Exception e){
      ExcpReporting.LogError(HelpTools.class, e);
      return;
    }
    
    QueryManager qb = new QueryManager();
    qb.addQuery(provider);
    qb.resetPassword(connectionProps);
  }

  public static void disconnectAllUsers(String bin1Cdir)
  {
    String test = "C:\\Program Files (x86)\1cv8\\8.3.7.1853\\bin";
    RASV83 clusterTool = new  RASV83();
    clusterTool.connectCluster(test);
  }
  */
}
