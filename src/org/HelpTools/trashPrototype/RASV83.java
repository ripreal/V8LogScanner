package org.HelpTools.trashPrototype;

public class RASV83 {
/*
  public void connectCluster (String bin1Cdir)
  {
    String exeRAC = "";
    if (Constants.osType.matches("(?i)Linux"))
      exeRAC = bin1Cdir + "\\rac";
    else
      exeRAC = bin1Cdir + "\\rac.exe";
    
    String clusterCmd = " cluster list";
        
    ProcessBuilder cluster = new ProcessBuilder();        
    cluster.command(exeRAC, clusterCmd);
  }  
*/
}
